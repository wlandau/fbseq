#' @title Function \code{sampler_options}
#' @description Returns the vector of options for slot of the \code{Samplers} object
#' @export
#' @return vector of options for slot of the \code{Samplers} object
sampler_options = function(){
  c("default", "slice_step")
}
